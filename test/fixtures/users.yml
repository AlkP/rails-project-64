# == Schema Information
#
# Table name: users
#
#  id                   :integer          not null, primary key
#  email                :string
#  name                 :string
#  encrypted_password   :string
#  reset_password_token :string
#  created_at           :datetime         not null
#  updated_at           :datetime         not null
#
# Indexes
#
#  index_users_on_email                  (email)
#  index_users_on_reset_password_token   (reset_password_token)

admin: { email: 'test@ya.ru', encrypted_password: <%= BCrypt::Password.create('password', cost: 10).to_s %> }
user: { email: 'testN@ya.ru', encrypted_password: <%= BCrypt::Password.create('password', cost: 10).to_s %> }
